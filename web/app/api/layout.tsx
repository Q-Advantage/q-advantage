import { PublicToolLayout } from "@/components/chrome/PublicToolLayout";

export default function ApiLayout({ children }: { children: React.ReactNode }) {
  return <PublicToolLayout>{children}</PublicToolLayout>;
}
