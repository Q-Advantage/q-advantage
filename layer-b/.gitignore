# Captures and result files produced by a local run. Layer B results are
# committed deliberately, from the measurement host, never as a side effect of
# someone running compose on a laptop.
out/
*.pcap

# Python bytecode from local test runs.
__pycache__/
*.pyc
