# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
ENV/
*.egg-info/
.pytest_cache/
.mypy_cache/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Secrets — never commit these
.env*
!.env.example
!.env.*.example
*.pem
*.key
*.p12
*.pfx
aws-credentials*
.aws/
credentials.json
secrets.json

# Benchmark scratch
benchmark/scratch/
benchmark/*.log

# Benchmark run logs are regeneratable; keep results.json/md only
benchmark/results/run.log
benchmark/results/*.log

# context/ is a junction to the founder's vault (see docs/adr/0002) — vault
# content must never enter this public repo's git history.
context/
