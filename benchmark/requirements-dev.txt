# Test-only dependencies for the harness. Deliberately does not include oqs,
# psutil or tabulate: CI has no liboqs build, so the tests here cover the pure
# logic that does not need one (status classification, statistics, result
# shaping). Anything that genuinely needs liboqs is verified by the daily run
# on the measurement host, not here — see benchmark/tests/README.md.
pytest>=8.0
jsonschema>=4.0
cryptography>=42.0
pyyaml>=6.0
